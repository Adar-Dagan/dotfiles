# Add rebuild script to path
export PATH=$PATH:~/dotfiles/nixos
# Add restow script to path
export PATH=$PATH:~/dotfiles/stow
export PATH=$PATH:~/zig_compiler

eval "$(zoxide init bash)"

eval "$(pay-respects bash --alias)"
